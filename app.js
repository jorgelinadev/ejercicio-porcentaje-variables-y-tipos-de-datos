//alert ("Mi archivo está bien linkeado")

const numero = prompt ('Ingresa el número al que quiera calcularle el porcentaje');
const porcentaje = prompt ('Ingresa el valor del porcentaje a calcular')

const resultado = (numero * porcentaje ) /100;

alert ('El' + porcentaje + '% de ' + numero + 'es: ' + resultado + '.');

console.log (alert);